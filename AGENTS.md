# AGENTS.md

이 파일은 저장소 전체에 적용되는 개발 에이전트 작업 규칙이다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 생기면 그 파일의 범위에서는 하위 규칙을 함께 적용한다.

## 1. 세션 시작 시 반드시 읽을 것

모든 개발 세션은 작업 전에 다음 파일을 순서대로 확인한다.

1. `AGENTS.md`
2. `NEXT_SESSION_PLAN.md`
3. `execution_log.csv`의 마지막 관련 기록
4. 현재 저장소 상태, 관련 소스, 테스트, 설정 파일

요청과 설계 문서가 충돌하면 사용자 요청을 우선하되, 개인정보 보호나 원본 손상 위험이 생기면 변경 전에 충돌과 대안을 명확히 보고한다.

## 2. 프로젝트 목표와 우선순위

이 프로젝트는 양식이 일정하지 않은 시험지 PDF에서 후발 손글씨를 제거하고 인쇄된 문제, 수식, 표, 그림과 페이지 레이아웃을 보존하는 로컬 우선 웹앱이다.

판단 우선순위:

1. 인쇄물 보존
2. 사용자 PDF의 개인정보 보호
3. 원본 파일 불변
4. 불확실한 영역의 검수 전환
5. 출력 PDF의 구조 및 시각 검증
6. 처리 속도와 자동 통과율

필수 원칙:

- 원본 PDF를 수정하거나 덮어쓰지 않는다.
- 사용자 PDF와 페이지 이미지를 기본적으로 외부 서버에 보내지 않는다.
- 인쇄물인지 필기인지 확신할 수 없으면 자동 삭제하지 않는다.
- 구조 보존 overlay 출력과 원본 답안을 실제로 제거한 보안 평탄화 출력을 구분한다.
- 큰 문서는 페이지 및 타일 단위로 처리한다.
- 출력 PDF를 다시 열고 렌더링해 검증하기 전에는 성공으로 판단하지 않는다.
- 모델이 복구한 내용을 원본과 동일하다고 과장하지 않는다.

자세한 제품 및 기술 설계는 `NEXT_SESSION_PLAN.md`를 따른다.

## 3. 필수 작업 루프

코드만 생성하고 종료하지 않는다. 모든 변경 작업은 다음 순서를 따른다.

### 3.1 관련 파일 찾기

- `rg --files`, `rg`, 저장소 상태, 설정 및 테스트를 먼저 확인한다.
- 변경 대상뿐 아니라 호출자, 타입, 테스트, 빌드 설정과 출력 경로를 함께 조사한다.
- 기존 구현 패턴과 라이브러리를 우선 사용한다.
- 파일명이나 API를 추측하지 않는다.

### 3.2 변경안 제시

- 편집 전에 사용자에게 1-3문장으로 변경 범위와 검증 방법을 알린다.
- 선택지가 결과나 호환성을 크게 바꾸는 경우에만 질문한다.
- 보통의 구현 세부 사항은 기존 코드와 설계를 근거로 합리적으로 결정한다.

### 3.3 수정

- 요청과 직접 관련된 파일만 최소 범위로 수정한다.
- 기존 사용자 변경을 되돌리거나 덮어쓰지 않는다.
- 수동 편집에는 `apply_patch`를 우선 사용한다.
- 새 추상화는 실제 중복이나 복잡성을 줄일 때만 추가한다.
- 실패 경로와 취소, 재시도, 임시 파일 정리를 함께 구현한다.

### 3.4 테스트 및 린트 실행

- 변경 범위에 맞는 단위 테스트, 통합 테스트, 타입 검사, 린트를 실행한다.
- PDF 변경은 재열기, 페이지 구조 비교, 재렌더링과 시각 확인을 포함한다.
- 프런트엔드 변경은 데스크톱과 모바일 주요 viewport에서 확인한다.
- 테스트를 실행할 수 없으면 생략하지 말고 원인, 미검증 범위, 사용자가 실행할 정확한 명령을 보고한다.
- 실패한 테스트를 숨기거나 기존 실패라고 단정하지 않는다. 로그와 변경 전 상태로 근거를 확인한다.

### 3.5 결과 요약과 다음 액션

최종 보고는 이 문서의 "Diff 중심 보고 형식"을 따른다. 작업 결과와 검증 상태를 먼저 말하고, 남은 위험과 다음 행동을 명확히 적는다.

## 4. 추측 금지 및 근거 규칙

- 모르는 내용은 가정하지 말고 관련 파일, 테스트, 명령 출력, 공식 문서를 확인한다.
- 동작을 설명할 때 가능하면 `파일 경로:줄 번호`, 테스트 이름 또는 로그 행을 근거로 든다.
- 최신 라이브러리 API, 라이선스, 브라우저 지원, SEO 정책처럼 변할 수 있는 정보는 공식 자료에서 확인한다.
- 검색 결과 요약보다 공식 문서, 원본 저장소, 표준 명세를 우선한다.
- 확인하지 못한 내용은 `확인되지 않음`, `가정`, `추정` 중 하나로 명시한다.
- 실제 측정값이 없는 성능, 정확도, 처리량을 보장하지 않는다.
- 존재하지 않는 검색량, 자동완성 결과, 사용자 행동 데이터를 만들어내지 않는다.

## 5. execution_log.csv 기록 규칙

모든 작업은 저장소 루트의 `execution_log.csv`에 기록한다. 기존 행을 수정하거나 정렬하지 않고 파일 끝에 추가한다.

### 5.1 필수 기록 시점

- 관련 파일 조사 후, 첫 편집 전에 `started` 행을 추가한다.
- 작업 완료 후 검증 결과를 포함한 `succeeded` 행을 추가한다.
- 일부만 완료했으면 `partial`, 실패했으면 `failed`, 사용자 입력 없이는 진행할 수 없으면 `blocked` 행을 추가한다.
- 장시간 작업에서 중요한 단계가 끝나면 `checkpoint` 행을 추가할 수 있다.

### 5.2 CSV 스키마

```text
timestamp,session_id,task_id,actor,phase,status,summary,files_changed,commands,tests,evidence,risks,rollback,next_action
```

필드 규칙:

- 모든 필드는 큰따옴표로 감싼다.
- 필드 내부 큰따옴표는 `""`로 이스케이프한다.
- 한 필드 안의 여러 항목은 `; `로 구분하며 실제 줄바꿈을 넣지 않는다.
- `timestamp`는 timezone을 포함한 ISO 8601 형식을 사용한다.
- 한 세션의 모든 행은 같은 `session_id`를 사용한다.
- 하나의 사용자 목표는 같은 `task_id`를 사용한다.
- `commands`에는 중요한 조사, 빌드, 테스트 명령만 기록한다.
- `tests`에는 통과, 실패, 미실행과 이유를 기록한다.
- `evidence`에는 파일 경로, 테스트명, 렌더링 산출물 등 확인 근거를 기록한다.
- `rollback`에는 변경을 되돌릴 파일이나 안전한 역변경 방법을 기록한다.
- 비밀키, 토큰, 개인정보, PDF 본문, 학생 이름, 민감한 원본 파일명은 기록하지 않는다.
- 값이 없으면 빈 문자열을 사용한다. 열을 생략하지 않는다.

예시:

```csv
"2026-09-18T18:00:00+09:00","20260918-180000-kst","UI-001","Codex","implementation","started","PDF 검수 화면 구현 시작","","rg --files; rg -n review","not-run","NEXT_SESSION_PLAN.md","캔버스 상태 구조 확인 필요","변경 파일을 역패치","관련 컴포넌트 수정"
```

로그 자체를 변경한 사실도 완료 행의 `files_changed`에 포함한다. CSV가 잘못된 형식이 되지 않았는지 작업 종료 전에 파서로 확인한다.

## 6. Diff 중심 보고 형식

구현 작업의 최종 답변은 다음 순서를 기본으로 한다.

### 결과

완료, 부분 완료, 실패 중 하나와 사용자가 체감하는 결과를 짧게 설명한다.

### 변경 파일

- 새 파일과 수정 파일 목록
- 파일별 핵심 diff 요약
- 삭제된 파일이 있다면 삭제 이유와 복구 가능 여부

### 검증

- 실행한 테스트, 린트, 타입 검사, 빌드 명령
- PDF 재렌더링 또는 브라우저 시각 검증 결과
- 실행하지 못한 검증과 이유

### 리스크

- 알려진 side effect
- 성능, 호환성, 데이터 손실, 보안 및 라이선스 위험
- 불확실한 영역

### 롤백

- 되돌려야 할 파일과 설정
- 데이터 마이그레이션이 있으면 역마이그레이션 방법
- 원본 및 백업 위치

### 다음 액션

- 가장 가치가 높은 후속 작업 1-3개
- 사용자 결정이 필요한 내용

사소한 한 파일 변경은 짧게 보고할 수 있지만, 변경 파일, 검증, 리스크, 롤백 정보는 생략하지 않는다.

## 7. 고위험 변경 규칙

다음 변경은 고위험으로 취급한다.

- 인증과 권한
- 결제와 과금
- 개인정보 저장 및 전송
- 데이터베이스 schema와 migration
- 배포 및 인프라
- PDF 원본 삭제 또는 덮어쓰기
- 모델 weight, 학습 데이터, 라이선스 변경
- 보안 평탄화 및 민감 데이터 제거 로직

고위험 변경 시:

1. 영향 범위와 현재 동작을 근거로 확인한다.
2. 변경 전 검증 또는 재현 테스트를 만든다.
3. 최소 변경으로 구현한다.
4. 성공 경로와 실패 경로를 모두 테스트한다.
5. 되돌릴 수 있는 구체적인 방법을 보고한다.
6. 데이터 손실이나 외부 공개가 발생할 수 있으면 실행 전에 사용자 승인을 받는다.

## 8. PDF 및 영상 처리 규칙

- 분석 전 PDF 객체, 텍스트, 이미지, annotation 구조를 조사한다.
- 객체 수준 제거가 가능하면 영상 평탄화보다 우선한다.
- 렌더링과 PDF 좌표 사이 변환을 테스트한다.
- 제거 마스크 바깥 픽셀은 가능한 한 변경하지 않는다.
- OCR 상자 전체를 인쇄물로 간주하지 않는다. 상자 안의 실제 glyph와 구조를 보호한다.
- 모델 확신도가 낮거나 인쇄 획과 충돌하면 검수 상태로 보낸다.
- 대용량 이미지는 타일에 overlap을 두고 경계 artifact를 검사한다.
- 출력 페이지 수, MediaBox, CropBox, 회전을 입력과 비교한다.
- 보안 출력에는 원본 답안 annotation, 첨부 파일, 숨은 레이어 또는 원본 페이지 이미지가 남지 않았는지 확인한다.
- 테스트 fixture에는 디지털 PDF, 스캔 PDF, 회전 페이지, 표, 수식, 옅은 연필, 인쇄물과 겹친 필기를 포함한다.

## 9. 프런트엔드 규칙

- 첫 화면은 실제 PDF 처리 화면이어야 한다. 별도 마케팅 landing page를 기본 화면으로 만들지 않는다.
- 업로드, 분석, 검수, 내보내기의 진행 상태와 오류를 분명히 보여 준다.
- 긴 작업은 취소하고 재개할 수 있어야 한다.
- 페이지 이동 후에도 마스크 수정과 undo/redo 상태가 보존되어야 한다.
- 아이콘 버튼에는 기존 아이콘 라이브러리를 사용하고 의미가 불명확하면 tooltip을 제공한다.
- 고정 형식 캔버스와 toolbar는 안정적인 크기를 사용해 상태 변화로 layout이 흔들리지 않게 한다.
- 모바일과 데스크톱에서 텍스트 겹침, 잘림, 캔버스 오버플로를 확인한다.
- 외부 분석 스크립트, 원격 폰트, 사용자 파일을 전송하는 telemetry를 기본 포함하지 않는다.

## 10. 모델 및 데이터 규칙

- 모델 interface와 전처리/후처리를 분리해 heuristic과 ONNX 모델을 교체할 수 있게 한다.
- 모델 manifest에 버전, SHA-256, 입력 shape, class 목록, threshold와 라이선스를 기록한다.
- 학습/검증/테스트는 페이지가 아니라 원본 문서 단위로 분리한다.
- 사용자 수정 마스크를 학습에 사용할 때는 명시적으로 로컬 저장한 데이터만 사용한다.
- 학생 이름이나 원본 PDF를 실행 로그, fixture, 저장소에 넣지 않는다.
- 새 모델은 고정 검증 세트에서 인쇄 손상률과 필기 제거율을 기존 모델과 비교한다.
- 인쇄 손상률이 악화되면 자동 통과 threshold를 낮추거나 모델 배포를 중단한다.

## 11. SEO 작업 규칙

이 절은 공개적으로 검색 노출할 페이지를 만들거나 사용자가 SEO 작업을 요청할 때 적용한다. 로컬 처리 앱 내부 화면을 불필요한 SEO 문구로 채우지 않는다.

### 11.1 시작 전 입력 확인

다음 정보가 저장소와 사용자 요청에 없으면 사용자에게 확인한다.

- 메인 키워드
- 실제 배포 사이트 URL
- 타겟 사용자와 언어
- 기술 스택

### 11.2 메타데이터

- `<title>`은 한국어 기준 30-35자를 목표로 하고 메인 키워드를 앞쪽에 자연스럽게 배치한다.
- `<meta name="description">`은 70-120자를 목표로 검색 의도와 과장 없는 CTA를 포함한다.
- canonical URL을 설정한다.
- Open Graph와 Twitter `summary_large_image`를 설정한다.
- `og:image`는 1200 x 630 픽셀 이미지를 사용한다.
- 실제 페이지 내용과 다른 제목이나 설명을 사용하지 않는다.

### 11.3 구조화 데이터

- schema.org 공식 명세에 있는 타입만 사용한다.
- 서비스 성격에 따라 `WebApplication`, `Article`, `FAQPage`, `BreadcrumbList` 등을 선택한다.
- 페이지에 보이지 않는 FAQ나 평가 정보를 JSON-LD에만 넣지 않는다.
- 배포 전 구조화 데이터 validator로 검사한다.

### 11.4 robots.txt 및 sitemap.xml

- `robots.txt`에 명시적인 Allow/Disallow 정책과 Sitemap 절대 URL을 넣는다.
- `sitemap.xml`은 UTF-8로 생성한다.
- URL별 `lastmod`를 실제 변경 시각으로 관리한다.
- `changefreq`, `priority`가 프로젝트 정책상 필요하면 일관되게 포함한다.
- 개인정보가 있는 작업 URL, 임시 파일, API 경로는 색인하지 않는다.

### 11.5 키워드 리서치

표에 다음 내용을 정리한다.

- 메인 키워드
- LSI 또는 의미 연관 키워드 10개 이상
- 롱테일 키워드 10개 이상
- 네이버와 구글 연관 검색 후보 10개 이상
- 정보형, 탐색형, 거래형 검색 의도

자동완성이나 검색 결과는 가능한 경우 실제 네이버와 구글에서 조사하고 조사일과 근거를 남긴다. 실제 조회가 불가능하면 `연관 검색 후보`라고 표시하며 실제 자동완성 결과로 단정하지 않는다.

### 11.6 콘텐츠 SEO

- 페이지당 하나의 명확한 H1과 논리적인 H2-H3 구조를 사용한다.
- 키워드는 제목, 도입부, 본문, FAQ에 자연스럽게 배치한다.
- 1-2%는 점검 기준일 뿐 강제로 맞추지 않는다. 문맥과 가독성이 우선이다.
- 필요한 경우 과장 없는 인트로와 실제 콘텐츠에 포함할 FAQ 3-5개를 작성한다.
- 키워드 stuffing, cloaking, 숨김 텍스트, doorway page를 사용하지 않는다.
- `100% 정확`, `무조건`, 검증되지 않은 `최고` 같은 보장성 표현을 피한다.

### 11.7 기술 SEO

- 목표 Core Web Vitals: LCP < 2.5초, INP < 200ms, CLS < 0.1
- 이미지 크기 지정, 적절한 `alt`, lazy loading과 압축을 적용한다.
- 모바일 사용성, semantic HTML, 내부 링크, status code를 확인한다.
- Google Search Console과 네이버 서치어드바이저 등록 절차를 배포 문서에 포함한다.
- 측정하지 않은 Core Web Vitals 수치를 달성했다고 보고하지 않는다.

### 11.8 SEO 산출물 보고 형식

각 산출물은 다음 구조로 제공한다.

1. 복사해 적용할 수 있는 코드 또는 텍스트
2. 적용 파일과 위치
3. 선택 근거와 검증 방법

## 12. 완료 정의

작업은 다음 조건을 모두 만족할 때 완료다.

- 요청된 동작이 구현됐다.
- 관련 테스트, 타입 검사, 린트 및 빌드가 통과했다.
- 사용자 흐름 또는 PDF 결과를 실제로 실행해 확인했다.
- 알려진 위험과 미검증 범위를 보고했다.
- 롤백 방법을 설명했다.
- `execution_log.csv`에 완료 상태를 추가했다.
- 문서와 실제 구현이 일치한다.

