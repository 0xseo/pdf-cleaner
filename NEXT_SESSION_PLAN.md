# 비정형 시험지 필기 제거기 - 다음 세션 구현 설계서

## 1. 최종 결정

이 프로젝트는 단순한 "답안 칸 지우기" 도구가 아니다. 시험지 양식이 매번 다르고 문제 본문 위에도 필기가 있을 수 있으므로, 페이지 전체를 대상으로 다음 성분을 분리하는 문서 영상 처리 시스템으로 만든다.

- 인쇄된 글자, 수식, 그림, 표, 밑줄은 보존한다.
- 학생이 나중에 쓴 연필, 볼펜, 체크, 동그라미, 풀이 흔적은 제거한다.
- 인쇄물과 필기가 접촉하거나 겹쳐 확신할 수 없는 부분은 자동으로 지우지 않고 검수 대상으로 보낸다.
- PDF는 외부 서버로 전송하지 않는다.
- 앱 자체에는 페이지 수, 처리 횟수, 업로드 용량에 따른 과금이나 인위적인 제한을 두지 않는다.
- 큰 PDF도 문서 전체를 메모리에 올리지 않고 페이지와 타일 단위로 처리한다.

가장 좋은 제품 형태는 **로컬 우선 웹앱**이다.

1. 사용자는 일반 웹사이트처럼 브라우저에서 앱을 연다.
2. 기본 모드는 브라우저 안에서 PDF를 처리하는 PWA이다.
3. 고품질 OCR, 빠른 GPU 처리, 대용량 PDF가 필요하면 동일한 화면이 사용자 PC의 로컬 Python 엔진에 연결된다.
4. 공개 서버는 정적 HTML, JavaScript, ONNX 모델만 배포한다. 사용자 PDF는 업로드하지 않는다.
5. 학교나 기관에서는 같은 앱과 엔진을 내부 PC 또는 사내 서버에 Docker로 설치할 수 있다.

이 구조이면 공개 URL을 제공할 수 있고, 서버 GPU 비용과 파일 저장 비용 없이 처리량을 사용자 기기 성능에 맡길 수 있다. 여기서 "용량 제한 없음"은 클라우드 서비스의 요금제 제한이 없다는 뜻이다. 실제 한계는 사용자 디스크, 메모리, 브라우저 및 운영체제 자원이다.

## 2. 반드시 인정해야 할 한계

다음 두 경우에는 어떤 모델도 원본을 100% 정확히 복구한다고 보장할 수 없다.

### 2.1 필기가 인쇄 글자를 완전히 덮은 경우

스캔 이미지에서 연필이나 펜이 인쇄 획을 덮으면 그 아래 원래 픽셀은 이미 관측되지 않는다. 모델은 주변 문맥으로 추정할 수는 있지만 원본과 동일하다고 증명할 수 없다.

기본 정책은 보수적으로 동작해야 한다.

- 인쇄 획과 겹치지 않은 필기는 자동 제거한다.
- 인쇄 획과 닿은 필기는 확신도가 높을 때만 제거한다.
- 인쇄 획을 훼손할 위험이 있으면 해당 부분을 남기고 검수 목록에 표시한다.
- 사용자가 강제 제거를 선택한 경우에만 주변 배경 또는 선 구조를 복원한다.

### 2.2 학생 필기와 교사 필기를 사람별로 구별해야 하는 경우

단일 문서의 모양만으로 "학생이 쓴 것"과 "교사가 쓴 것"을 항상 판별할 수는 없다. 둘 다 손글씨이기 때문이다. 교사 표시를 남겨야 한다면 색상, 펜 굵기, 별도 예시 또는 사용자 지정 보존 마스크가 필요하다.

초기 제품의 정확한 범위는 **모든 후발 손글씨 제거**로 잡는다. 작성자 신원 분리는 별도 기능으로 둔다.

## 3. 전체 구조

```text
공개 정적 웹사이트 / 설치형 PWA
  - React + TypeScript
  - PDF.js 렌더링
  - Web Worker + OffscreenCanvas
  - ONNX Runtime Web
  - 마스크 검수 편집기
  - pdf-lib 출력
            |
            | 선택적 localhost 연결
            v
로컬 고품질 엔진
  - FastAPI
  - pypdfium2 + pypdf + pikepdf
  - OpenCV
  - PaddleOCR 텍스트 검출
  - ONNX Runtime / PyTorch
  - CPU, Apple Silicon, CUDA 가속
```

### 권장 동작 모드

| 모드 | 파일 이동 | 장점 | 용도 |
|---|---|---|---|
| 브라우저 전용 | 없음 | 설치 없이 공개 URL에서 사용, 서버 비용 없음 | 소량 및 중간 크기 PDF |
| 로컬 엔진 연결 | localhost 안에서만 이동 | 가장 높은 품질, 대용량, GPU, 모델 학습 가능 | 기본 권장 모드 |
| 기관 내부 서버 | 내부망 안에서 이동 | 여러 사용자가 한 장비를 공유 | 학교 및 학원 |
| 일반 클라우드 업로드 | 외부 서버로 업로드 | 설치가 쉬움 | 무료 무제한 목표와 맞지 않아 기본 범위에서 제외 |

## 4. 페이지별 처리 파이프라인

모든 페이지에 같은 영상 알고리즘을 강제로 적용하지 않는다. 먼저 PDF 내부 구조를 조사하고 가장 손실이 적은 분기를 선택한다.

### 4.1 PDF 사전 검사

페이지마다 다음 정보를 수집한다.

- 페이지 크기, 회전, CropBox, MediaBox
- 텍스트 객체 수와 좌표
- 이미지 XObject 수, 크기, 투명도, 배치 영역
- 벡터 경로 수와 선 굵기
- `/Ink`, `/FreeText`, `/Stamp` 등의 주석
- 페이지가 디지털 원본인지 전체 스캔 이미지인지

페이지 유형은 다음과 같이 분류한다.

1. **주석 필기형**: 손글씨가 PDF Ink annotation으로 존재한다.
2. **분리 레이어형**: 인쇄 원본과 손글씨 이미지 또는 벡터가 별도 객체다.
3. **디지털 혼합형**: 인쇄 텍스트는 PDF 객체지만 일부 필기가 같은 렌더 결과에 섞여 있다.
4. **전체 스캔형**: 페이지가 사실상 한 장의 이미지다.
5. **불명확형**: 손상된 PDF, 특수 인코딩, 복잡한 투명도 등이 있다.

가능하면 객체 수준에서 필기만 삭제한다. 불가능할 때만 영상 분할로 내려간다.

### 4.2 페이지 렌더링

- 기본 분석 해상도: 300 DPI
- 작은 글자, 수식, 얇은 연필: 400 DPI 재분석
- 최종 보안 평탄화: 사용자 선택에 따라 400 또는 600 DPI
- 색 공간: 원본 RGB와 Lab 또는 grayscale 파생 채널
- 기울기와 회전은 분석용 이미지에서만 보정하고 출력 좌표는 원래 PDF 좌표로 역변환한다.
- 큰 페이지는 1024 x 1024 타일, 128 픽셀 겹침으로 처리한다.

### 4.3 인쇄물 보존 마스크 생성

보존 마스크는 한 가지 OCR 결과만 신뢰하지 않고 여러 근거를 합친다.

- PDF 텍스트 객체의 정확한 좌표
- OCR 텍스트 검출 상자
- 모델이 예측한 printed-content 확률
- 연결 성분의 반복적인 글꼴 획 굵기
- 수평선, 수직선, 표 테두리, 답안 밑줄
- 수식과 도형의 안정적인 에지
- 로고, 삽화, 사진 영역

OCR 상자 전체를 보호하면 그 안에 쓴 필기를 지울 수 없으므로, 상자는 위치 힌트로만 사용한다. 실제 보호는 상자 안의 인쇄 획 픽셀까지 좁혀 만든다. 인쇄 획 주변은 300 DPI 기준 1-2 픽셀 정도 팽창시켜 손상을 막는다.

### 4.4 필기 분할

핵심 모델은 객체 탐지가 아니라 픽셀 단위 semantic segmentation 모델이다.

권장 클래스:

```text
0 background
1 printed_content
2 handwriting
3 structural_line
4 ambiguous_mark
```

권장 모델 구성:

- 브라우저/저사양: MobileNetV3 encoder + U-Net 또는 DeepLabV3+ decoder
- 데스크톱 고품질: SegFormer-B1/B2급 모델 또는 더 큰 U-Net 계열
- 배포 포맷: ONNX
- 브라우저: ONNX Runtime Web, WebGPU 우선, WASM fallback
- 로컬 엔진: ONNX Runtime CPU/CoreML/CUDA 또는 PyTorch MPS/CUDA
- INT8 양자화 모델은 속도용으로 제공하되, 정확도 검증을 통과한 경우에만 기본값으로 사용한다.

SAM 계열 모델은 사용자가 클릭한 영역을 정교하게 다듬는 보조 도구로는 유용하지만, 손글씨와 인쇄물을 의미적으로 자동 구별하는 핵심 모델로 사용하지 않는다.

### 4.5 규칙 기반 후처리

모델 확률과 고전 영상 처리를 결합한다.

예시 초기 임계값:

```text
자동 제거:
  P(handwriting) >= 0.75
  P(handwriting) - P(printed_content) >= 0.25
  hard preserve mask와 충돌하지 않음

검수 대상:
  0.45 <= P(handwriting) < 0.75
  또는 인쇄 획과 접촉
  또는 표/수식/그림 경계와 접촉

자동 보존:
  P(printed_content) >= 0.80
  또는 PDF 텍스트 객체의 glyph core
```

이 숫자는 출발점일 뿐이며 검증 세트로 calibration한다.

후처리 항목:

- 너무 작은 먼지와 스캔 노이즈 제외
- 끊어진 연필 획 연결
- 체크, 동그라미, 화살표, 취소선 형태의 긴 성분 유지
- 표 선과 필기의 교차점에서 선 구조 보호
- 문서 가장자리 그림자와 펀치 구멍 제외
- 후보 연결 성분별 확신도와 위험도 계산

최종 제거 마스크는 `ink candidate AND handwriting confidence AND NOT hard preserve` 원칙으로 만든다.

### 4.6 배경 복원

모든 제거 영역을 순백색 사각형으로 덮으면 스캔 배경과 표 선이 깨진다. 영역 특성에 따라 복원 방법을 바꾼다.

- 균일한 흰 배경: 주변 밝기의 상위 분위값으로 채움
- 회색 또는 누런 스캔 배경: 주변 저주파 배경을 보간
- 얇은 필기: OpenCV Telea/Navier-Stokes inpainting 후보 비교
- 표나 밑줄 위 필기: 먼저 필기 제거 후 검출된 선을 다시 이어 그림
- 인쇄 획과 겹침: 자동 복원 금지, 검수 또는 강제 복원
- 큰 낙서: patch 기반 복원 또는 사용자 검수

복원 후 원래 마스크 바깥 픽셀은 바뀌지 않았는지 반드시 비교한다.

### 4.7 페이지 위험도 계산

연결 성분마다 다음 위험 요소를 계산한다.

- 모델 entropy
- 인쇄 보존 마스크와의 겹침 비율
- OCR 글자 상자와의 거리
- 표/수식/그림과의 접촉
- 성분 크기와 비정상적인 폭
- 복원 전후 경계의 불연속

페이지에는 `자동 통과`, `검수 권장`, `수동 확인 필수` 중 하나의 상태를 부여한다. 자동화 목표는 모든 페이지를 억지로 처리하는 것이 아니라, 안전한 페이지는 자동 통과시키고 위험한 페이지만 빠르게 보여 주는 것이다.

## 5. 출력 PDF 전략

### 5.1 구조 보존 출력

원본 PDF 위에 필기 픽셀만 가리는 투명 correction layer를 합성한다.

장점:

- 원본 벡터 글자와 검색 가능한 텍스트가 유지된다.
- 변경되지 않은 영역은 원본 그대로다.
- 파일 크기가 비교적 작다.

주의:

- 원래 필기가 PDF 내부에 남아 있어 correction layer를 제거하면 다시 보일 수 있다.
- 제출용이나 개인정보 삭제용으로는 안전하지 않다.

### 5.2 보안 평탄화 출력

수정 결과를 새 페이지 이미지로 만들고 새 PDF를 생성한다. 원본 주석, 첨부 파일, 숨은 레이어, 이미지 XObject를 복사하지 않는다. 필요하면 OCR 결과를 보이지 않는 검색 텍스트 레이어로 추가한다.

장점:

- 지운 답안이 PDF 내부에 남지 않는다.
- 어떤 뷰어에서도 같은 모습으로 보인다.

단점:

- 원래 벡터 텍스트가 이미지화된다.
- 400-600 DPI에서는 파일 크기가 커질 수 있다.

기본 제품은 두 출력을 모두 제공하되, 사용자에게 배포할 최종본은 **보안 평탄화 출력**을 기본으로 한다. 객체 수준에서 필기만 완전히 제거할 수 있는 페이지는 벡터 원본을 유지해도 된다.

출력 파일명:

```text
{원본명}_cleaned.pdf
{원본명}_review.json
```

`review.json`에는 원본 PDF 해시, 모델 버전, 페이지별 마스크, 사용자 수정 기록, 위험도, 출력 옵션을 저장한다. 원본 답안 이미지는 저장하지 않는다.

## 6. 검수 웹 UI

검수 화면은 정확도만큼 중요하다. 자동 결과가 완벽하지 않아도 사용자가 한 페이지를 몇 초 안에 확인하고 고칠 수 있어야 한다.

필수 기능:

- 왼쪽 페이지 썸네일과 위험도 표시
- 중앙 원본/처리 결과 전환 및 before-after 슬라이더
- 필기 후보 마스크와 인쇄 보호 마스크 색상 오버레이
- 확대, 이동, 맞춤 보기
- 제거 브러시, 보존 브러시, 지우개
- 연결 성분 클릭 선택과 일괄 승인/거부
- 실행 취소와 다시 실행
- 위험 영역 사이 빠른 이동
- 페이지 재처리와 임계값 조정
- 처리 취소 및 이어하기
- 세션 저장과 재개
- 최종 PDF 내보내기 전 미리보기

대용량 문서는 현재 페이지와 인접 페이지만 디코딩한다. 각 페이지 결과는 즉시 IndexedDB 또는 로컬 엔진 작업 폴더에 기록해 브라우저가 닫혀도 재개할 수 있게 한다.

## 7. 데이터와 모델 학습 전략

처음부터 범용 필기 제거 모델을 찾기보다, 합성 데이터와 실제 사용자 수정 데이터를 결합해 이 작업에 맞는 모델을 만든다.

### 7.1 합성 학습 데이터

사용 권한이 있는 깨끗한 시험지에 다음 항목을 무작위로 합성한다.

- 연필, 볼펜, 형광펜 색과 압력
- 한글, 영문, 숫자, 수식, 체크, 화살표, 동그라미
- 자유 곡선, 취소선, 밑줄, 풀이 과정
- 다양한 굵기, 투명도, 회전, 흐림
- JPEG 압축, 스캔 노이즈, 그림자, 기울기
- 인쇄 글자 및 표 선과 의도적으로 겹치는 예시

합성 과정에서 필기 마스크가 자동으로 생성되므로 많은 데이터를 무료로 만들 수 있다. 손글씨 폰트만 사용하면 실제 획 다양성이 부족하므로, 사용자가 빈 종이에 쓴 낙서 샘플과 절차적으로 생성한 곡선도 함께 사용한다.

### 7.2 실제 데이터 active learning

1. 초기 모델로 실제 문서를 처리한다.
2. 확신이 낮은 타일만 사용자에게 보여 준다.
3. 사용자가 고친 마스크를 로컬 학습 데이터로 저장한다.
4. 일정량이 쌓이면 모델을 미세 조정한다.
5. 새 모델과 기존 모델을 고정 검증 세트에서 비교한다.
6. 인쇄 손상률이 나빠진 모델은 배포하지 않는다.

데이터 분할은 페이지 단위가 아니라 **원본 문서 단위**로 한다. 같은 시험지의 유사한 페이지가 학습과 검증에 동시에 들어가면 성능이 과대평가된다.

### 7.3 hard negative

모델이 필기로 오인하기 쉬운 다음 사례를 별도로 수집한다.

- 기울임꼴 글자
- 손글씨처럼 보이는 인쇄 폰트
- 수학 기호와 그래프
- 표의 얇은 선과 점선
- 인쇄된 밑줄, 동그라미, 체크 표시
- 사진과 삽화의 가는 선
- 복사기 얼룩과 종이 주름

## 8. 품질 지표와 합격 기준

정확도를 "보기 좋아 보임"으로만 판단하지 않는다. 합성 정답과 수동 라벨 검증 세트로 측정한다.

핵심 지표:

- handwriting pixel precision, recall, F1
- printed-content damage rate
- 비중첩 필기 잔존율
- 인쇄 충돌 영역 검수 회수율
- 마스크 바깥 픽셀 변경률
- 페이지별 자동 통과 비율
- 한 페이지 처리 시간과 최대 메모리
- 출력 PDF 재렌더링 차이

초기 release gate 제안:

```text
인쇄 픽셀 손상률 <= 0.05%
비중첩 필기 제거 recall >= 97%
인쇄 충돌 고위험 영역 검수 회수율 >= 99%
마스크 외부 픽셀 변경률 <= 0.01%
출력 PDF 페이지 수, 크기, 회전 100% 일치
손상되거나 지원하지 않는 페이지는 조용히 실패하지 않고 검수 상태로 전환
```

수치는 실제 검증 세트 난이도에 따라 조정할 수 있지만, **인쇄 손상률을 필기 제거율보다 우선**한다.

## 9. PDF 검증 절차

출력 후 반드시 다음 검사를 자동 실행한다.

1. `pypdf`로 다시 열어 파일 손상 여부 확인
2. 입력과 출력의 페이지 수 비교
3. 각 페이지 MediaBox, CropBox, 회전 비교
4. 모든 페이지를 저해상도로 재렌더링해 빈 페이지와 깨진 렌더 탐지
5. 위험 페이지는 300 DPI 이상으로 재렌더링
6. 제거 마스크 바깥의 입력/출력 픽셀 차이 측정
7. 표 선, 페이지 번호, 머리말, 꼬리말 손상 검사
8. 보안 출력에서 원본 Ink annotation, 첨부 파일, 원본 페이지 이미지가 남지 않았는지 검사
9. 검색 텍스트 레이어를 넣은 경우 페이지 시각 결과와 텍스트 좌표가 어긋나지 않는지 확인

시각 QA를 통과하지 않은 파일은 성공으로 표시하지 않는다.

## 10. 권장 기술 스택

### 프런트엔드

- React + TypeScript + Vite
- PDF.js: 브라우저 PDF 렌더링
- ONNX Runtime Web: WebGPU/WASM 추론
- Konva 또는 Fabric.js: 마스크 편집 캔버스
- pdf-lib: 브라우저 PDF 생성과 페이지 합성
- IndexedDB: 작업 상태와 마스크 저장
- Service Worker: 오프라인 실행과 모델 캐시

### 로컬 엔진

- Python 3.11+
- FastAPI + Uvicorn
- pypdfium2: PDF 렌더링
- pypdf + pikepdf: PDF 구조 검사와 출력
- OpenCV + NumPy + Pillow
- PaddleOCR text detector: 인쇄 텍스트 위치 보조
- ONNX Runtime
- PyTorch: 학습과 모델 변환에만 사용

배포물의 라이선스는 코드뿐 아니라 모델 weight와 폰트까지 각각 확인한다. AGPL 의존성을 피하고 싶다면 PyMuPDF/MuPDF를 기본 스택에 넣지 않는다. PaddleOCR은 Apache-2.0, ONNX Runtime과 pdf-lib은 MIT이며, 실제 배포 시 각 프로젝트의 LICENSE와 ThirdPartyNotices를 포함한다.

공식 참고 자료:

- PaddleOCR PP-OCRv5: https://www.paddleocr.ai/main/en/version3.x/algorithm/PP-OCRv5/PP-OCRv5.html
- ONNX Runtime Web: https://onnxruntime.ai/docs/get-started/with-javascript/web.html
- ONNX Runtime Mobile: https://onnxruntime.ai/docs/get-started/with-mobile.html
- PDF.js: https://github.com/mozilla/pdf.js
- pdf-lib: https://github.com/Hopding/pdf-lib

## 11. 제안 저장소 구조

```text
pdf-cleaner/
  apps/
    web/
      src/
        components/
        workers/
        inference/
        pdf/
        editor/
  services/
    local-engine/
      app/
        api/
        pdf/
        detection/
        restoration/
        export/
        qa/
  ml/
    datasets/
    synthetic/
    training/
    evaluation/
    export_onnx/
  models/
    manifest.json
  tests/
    fixtures/
    golden/
    e2e/
  docs/
  docker/
```

모델 파일 자체는 저장소에 바로 커밋하지 않고, 버전과 SHA-256, 입력 크기, 클래스 목록, 라이선스를 `models/manifest.json`으로 관리한다.

## 12. 로컬 API 초안

```text
GET  /api/health
POST /api/jobs
GET  /api/jobs/{job_id}
POST /api/jobs/{job_id}/pages/{page}/analyze
PATCH /api/jobs/{job_id}/pages/{page}/mask
POST /api/jobs/{job_id}/export
GET  /api/jobs/{job_id}/download
DELETE /api/jobs/{job_id}
```

원칙:

- job 디렉터리는 무작위 ID로 생성한다.
- 원본 파일명을 경로로 직접 사용하지 않는다.
- 업로드는 디스크로 streaming하고 전체 파일을 RAM에 올리지 않는다.
- localhost 외부 바인딩은 기본 비활성화한다.
- 일정 시간 후 임시 파일을 자동 삭제하되, 실행 중 작업은 지우지 않는다.
- 사용자가 명시적으로 저장한 검수 세션만 유지한다.

## 13. 웹사이트 배포 시나리오

### 공개 PWA

- 정적 호스팅에 `apps/web/dist`만 배포한다.
- PDF와 페이지 이미지는 네트워크 요청 본문에 넣지 않는다.
- 모델은 첫 실행 때 내려받아 브라우저 캐시에 저장한다.
- 이후 오프라인에서도 실행 가능하게 한다.
- 외부 CDN, 분석 스크립트, 원격 폰트는 사용하지 않는다.
- CSP를 설정해 임의 외부 전송을 막는다.

정적 호스팅 사업자에도 빌드 횟수나 트래픽 정책은 있을 수 있다. 앱 처리 횟수와 PDF 크기에는 서비스 측 제한이 없지만, 정적 파일 전송까지 문자 그대로 무제한이라고 홍보해서는 안 된다. 완전히 독립적으로 운영하려면 정적 파일도 개인 서버나 학교 내부 서버에 self-host한다.

### 로컬 엔진

- 한 번 설치한 사용자는 `http://127.0.0.1`에서 앱을 열 수 있다.
- 공개 PWA가 localhost health check를 통해 엔진을 발견할 수 있다.
- 엔진이 없으면 브라우저 모드로 자동 fallback한다.
- Docker와 일반 Python 실행 방식을 모두 제공한다.

### 기관 내부 배포

- Docker Compose로 웹과 엔진을 내부 GPU PC에 배포한다.
- 외부 인터넷 없이 모델 파일까지 함께 제공한다.
- 작업 파일은 기관이 지정한 로컬 볼륨에만 저장한다.

## 14. 구현 단계

### 1단계 - 실행 가능한 골격

- React/Vite 웹 UI와 FastAPI 로컬 엔진 생성
- PDF 업로드, 페이지 썸네일, 300 DPI 렌더링
- 페이지 단위 job 처리와 진행률
- 수동 마스크 편집
- 마스크 기반 PDF 출력
- PDF 재열기 및 페이지 구조 검증

이 단계에서는 모델 정확도보다 end-to-end 흐름을 완성한다.

### 2단계 - baseline 자동 검출

- PDF 객체/주석 검사
- OCR 텍스트 검출과 표 선 보존
- OpenCV 기반 잉크 후보 검출
- 위험도 계산과 검수 큐
- 구조 보존/보안 평탄화 두 출력

### 3단계 - 필기 분할 edge model

- 합성 데이터 생성기
- MobileNetV3 U-Net baseline 학습
- 문서 단위 train/validation/test 분리
- ONNX export와 브라우저/로컬 추론 일치 검사
- 모델 확률 calibration

### 4단계 - active learning과 품질 고도화

- 사용자가 수정한 마스크의 로컬 학습 데이터화
- hard-negative 수집
- 자동 회귀 평가
- 저사양/고품질 모델 선택
- 브라우저 WebGPU와 CPU 성능 최적화

### 5단계 - 배포

- PWA 오프라인 캐시
- Docker 이미지
- macOS/Windows 로컬 실행 패키지
- 정적 웹사이트 배포
- 라이선스 고지와 개인정보 설명

## 15. 다음 세션의 구체적 작업 범위

다음 세션에서는 1단계와 2단계의 최소 기능을 구현한다. 학습 모델이 아직 없어도 수동 검수와 OpenCV baseline으로 실제 PDF를 끝까지 처리할 수 있어야 한다.

완료 조건:

- PDF를 선택하면 페이지가 순차적으로 렌더링된다.
- 페이지 전체에서 잉크 후보가 표시된다.
- OCR/텍스트/표 선 보호 영역이 별도 색으로 표시된다.
- 사용자가 브러시로 제거/보존 마스크를 고칠 수 있다.
- 수정 상태가 페이지 이동 후에도 유지된다.
- 원본과 같은 페이지 크기의 cleaned PDF를 생성한다.
- 출력 PDF를 다시 열고 전 페이지 렌더 검증한다.
- 원본 PDF는 수정하거나 덮어쓰지 않는다.
- 작은 fixture PDF에 대한 자동 테스트가 있다.

## 16. 다음 세션에 붙여 넣을 요청문

```text
현재 작업 폴더의 NEXT_SESSION_PLAN.md를 먼저 모두 읽고 그 설계를 기준으로 구현을 시작해 주세요.

이번 세션 목표는 1단계 실행 가능한 골격과 2단계 baseline 자동 검출입니다. React + TypeScript + Vite 웹 UI, FastAPI 로컬 엔진, pypdfium2/pypdf/pikepdf/OpenCV 기반으로 구성해 주세요. PDF는 외부 서버로 전송하지 않고 localhost에서 페이지 단위로 처리해야 합니다.

필수 기능은 PDF 업로드, 페이지 렌더링, 썸네일, 잉크 후보 마스크, 인쇄 텍스트와 표 선 보호 마스크, 제거/보존 브러시, undo/redo, 페이지별 상태 저장, 구조 보존 출력과 보안 평탄화 출력, 출력 PDF 재검증입니다.

먼저 현재 저장소 상태와 사용 가능한 런타임을 조사한 뒤 구현하세요. 원본 파일은 절대 덮어쓰지 말고, 테스트용 fixture와 자동 테스트를 추가하세요. 구현 후 실제 샘플 PDF가 있으면 end-to-end로 실행하고 렌더링한 PNG를 시각 검사해 주세요. 모델 학습은 이번 세션 범위가 아니지만 이후 ONNX segmentation 모델을 끼울 수 있도록 inference interface를 분리해 주세요.
```

## 17. 최우선 판단 원칙

1. 인쇄물 보존이 필기 제거율보다 우선이다.
2. 확신이 낮으면 지우지 않고 검수로 보낸다.
3. 원본 PDF는 절대 덮어쓰지 않는다.
4. 사용자 파일은 기본적으로 기기 밖으로 보내지 않는다.
5. 대용량 문서는 페이지와 타일 단위로 처리한다.
6. 결과를 재렌더링하고 확인하기 전에는 성공으로 표시하지 않는다.
7. 지운 답안이 내부에 남을 수 있는 overlay 출력과, 실제로 제거한 보안 출력을 명확히 구분한다.

